# Dependencies (regenerated by `npm install`)
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store

# Local backup made by `npm run prune`
data/beers.full.json
public/data/beers.full.json

# Debug screenshot from `npm run debug`
debug-page.png

# Hop-gap worklist — regenerated every `npm run build` / `npm run gaps`
hop-gaps.txt

# Catalogue crawl diagnostic screenshots (one per shop)
catalog-page-1.png
catalog-*-page1.png

# NOTE: public/data/catalog.json IS committed on purpose — it's the data
# the live static site serves. Rebuild it with `npm run catalog`.
