# Dependencies (regenerated by `npm install`)
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store

# Local backup made by `npm run prune`
data/beers.full.json
public/data/beers.full.json

# Debug screenshot from `npm run debug`
debug-page.png

# Catalogue crawl diagnostic screenshot
catalog-page-1.png

# NOTE: public/data/tesco-catalog.json IS committed on purpose — it's the
# data the live static site serves. Rebuild it with `npm run catalog`.
